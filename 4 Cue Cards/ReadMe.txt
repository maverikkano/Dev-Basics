-----------
|LEARNINGS|
-----------

CSS
===
1. box-sizing: border-box;	// The width and height properties (and min/max properties) includes content, padding and border
2. display: grid;
   grid-template-columns: 1fr 1fr;	// automatically calculates layout divisions when adjusting for gaps inside the grid.

JavaScript
==========
1. let details = data.map(function(item) {
	. . .
   }
2.info.innerHTML = details.join('\n');