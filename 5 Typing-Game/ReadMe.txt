-----------
|LEARNINGS|
-----------

HTML
====

1. $nbsp; Non-breaking space
--> Used to reserve space in an HTML tag


JaveScript
===
1. words = map((word)=> {
        return word.concat(" ");
    });
--> Need to return iterations

2. Template Strings
`${Printing words[index] }` 

3. array.join("<seperator>")
--> Returns array as string with the given seperator

4. <element_object>.focus()